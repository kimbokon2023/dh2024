<?php
$num = isset($row["num"]) ? $row["num"] : null;
$id = isset($row["id"]) ? $row["id"] : null;
$name = isset($row["name"]) ? $row["name"] : null;
$subject = isset($row["subject"]) ? $row["subject"] : null;
$content = isset($row["content"]) ? $row["content"] : null;
$regist_day = isset($row["regist_day"]) ? $row["regist_day"] : null;
$hit = isset($row["hit"]) ? $row["hit"] : null;
$is_html = isset($row["is_html"]) ? $row["is_html"] : null;
$noticecheck = isset($row["noticecheck"]) ? $row["noticecheck"] : null;
$searchtext = isset($row["searchtext"]) ? $row["searchtext"] : null;
?>