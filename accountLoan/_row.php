<?php
$num = isset($row['num']) ? $row['num'] : '';
$loanStartDate = isset($row['loanStartDate']) ? $row['loanStartDate'] : '';
$bank = isset($row['bank']) ? $row['bank'] : '';
$loanAmount = isset($row['loanAmount']) ? $row['loanAmount'] : '';
$content = isset($row['content']) ? $row['content'] : '';
$interestRate = isset($row['interestRate']) ? $row['interestRate'] : '';
$interestPaymentDate = isset($row['interestPaymentDate']) ? $row['interestPaymentDate'] : '';
$memo = isset($row['memo']) ? $row['memo'] : '';
$crew = isset($row['crew']) ? $row['crew'] : '';
$crewphone = isset($row['crewphone']) ? $row['crewphone'] : '';
$loanAccount = isset($row['loanAccount']) ? $row['loanAccount'] : '';
$interestAccount = isset($row['interestAccount']) ? $row['interestAccount'] : '';
$maturityDate = isset($row['maturityDate']) ? $row['maturityDate'] : '';
$is_deleted = isset($row['is_deleted']) ? $row['is_deleted'] : null ;
?>
