<?php
$num = isset($_REQUEST['num']) ? $_REQUEST['num'] : '';
$item_code = isset($_REQUEST['item_code']) ? $_REQUEST['item_code'] : '';
$item_name = isset($_REQUEST['item_name']) ? $_REQUEST['item_name'] : '';
$spec = isset($_REQUEST['spec']) ? $_REQUEST['spec'] : '';
$spec_info = isset($_REQUEST['spec_info']) ? $_REQUEST['spec_info'] : '';
$unit = isset($_REQUEST['unit']) ? $_REQUEST['unit'] : '';
$item_type = isset($_REQUEST['item_type']) ? $_REQUEST['item_type'] : '';
$set_flag = isset($_REQUEST['set_flag']) ? $_REQUEST['set_flag'] : '';
$stock = isset($_REQUEST['stock']) ? $_REQUEST['stock'] : '';
$prod_proc = isset($_REQUEST['prod_proc']) ? $_REQUEST['prod_proc'] : '';
$in_price = isset($_REQUEST['in_price']) ? $_REQUEST['in_price'] : '';
$in_price_vat = isset($_REQUEST['in_price_vat']) ? $_REQUEST['in_price_vat'] : '';
$out_price = isset($_REQUEST['out_price']) ? $_REQUEST['out_price'] : '';
$out_price_vat = isset($_REQUEST['out_price_vat']) ? $_REQUEST['out_price_vat'] : '';
$is_deleted = isset($_REQUEST['is_deleted']) ? $_REQUEST['is_deleted'] : '';
$searchtag = isset($_REQUEST['searchtag']) ? $_REQUEST['searchtag'] : '';
$update_log = isset($_REQUEST['update_log']) ? $_REQUEST['update_log'] : '';
?>
