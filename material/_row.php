<?php
$num = isset($row['num']) ? $row['num'] : '';
$item_code = isset($row['item_code']) ? $row['item_code'] : '';
$item_name = isset($row['item_name']) ? $row['item_name'] : '';
$spec = isset($row['spec']) ? $row['spec'] : '';
$spec_info = isset($row['spec_info']) ? $row['spec_info'] : '';
$unit = isset($row['unit']) ? $row['unit'] : '';
$item_type = isset($row['item_type']) ? $row['item_type'] : '';
$set_flag = isset($row['set_flag']) ? $row['set_flag'] : '';
$stock = isset($row['stock']) ? $row['stock'] : '';
$prod_proc = isset($row['prod_proc']) ? $row['prod_proc'] : '';
$in_price = isset($row['in_price']) ? $row['in_price'] : '';
$in_price_vat = isset($row['in_price_vat']) ? $row['in_price_vat'] : '';
$out_price = isset($row['out_price']) ? $row['out_price'] : '';
$out_price_vat = isset($row['out_price_vat']) ? $row['out_price_vat'] : '';
$is_deleted = isset($row['is_deleted']) ? $row['is_deleted'] : '';
$searchtag = isset($row['searchtag']) ? $row['searchtag'] : '';
$update_log = isset($row['update_log']) ? $row['update_log'] : '';
?>
